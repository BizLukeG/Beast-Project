using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BeastList : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        string[] Beasts = { "Pablo", "Blake", "Rolly" };
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
